services:
  redis:
    image: redis:8.0.3
    container_name: redis
    restart: on-failure
    ports:
      - "6379:6379"
    networks:
      - sandbox_net

networks:
  sandbox_net:
    driver: bridge